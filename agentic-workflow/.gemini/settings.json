{
  "tools": {
    "allowed": [
      "*"
    ]
  },
  "sandbox": {
    "enabled": true
  },
  "mcpServers": {
    "gcloud": {
      "httpUrl": "http://127.0.0.1:8080/sse",
      "description": "Provides tools for interacting with Google Cloud via gcloud.",
      "includeTools": ["gcloud"]
    },
    "terraform": {
      "description": "Provides a MCP Server for real-time access to current Terraform provider documentation, modules, and policies from the Terraform registry.",
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "terraform-mcp-server:dev"
      ]
    },
    "github": {
      "description": "Provides a MCP server for interacting with the GitHub API.",
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "$GITHUB_PAT"
      }
    }
  }
}