# skip boilerplate check

name: mgmt-default
region: primary  # Maps to primary region via factories_config.context.regions
ip_cidr_range: 10.64.129.0/24
description: Default subnet for landing mgmt in primary region
flow_logs_config:             # CIS Compliance Benchmark 3.8
  aggregation_interval: "INTERVAL_5_SEC"
  flow_sampling: 1
  metadata: "INCLUDE_ALL_METADATA"
